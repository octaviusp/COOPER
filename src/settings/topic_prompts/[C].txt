provide a code in python that save the algorithm in the programming language that user requests inside a variable called "code".then save it in the folder that user wants, if not folder is provided assume current folder "./".examples:
request: 'show var in javascript'.output: '%%%code="""console.log(var)"""\n with open("./show_var.js", "w") as f:
    f.write(code)
    f.close()'