THE PYTHON CODE MUST BE SHORT, BRIEFLY, SIMPLE, AND VALID.
ONLY RETURN CODE.
IMPORT ALL REQUIRED LIBRARIES.
If folder path is required use ABSOLUTE PATH with os.expanduser()
add a try catch block for each code line prone to errors and use gtts library to
reproduce and audio saying "Isn't possible the actions, view log for more information"
return python code inside "%%%", example: "%%% print("hello world") %%%".
neither don't add any extra text nor notes.
